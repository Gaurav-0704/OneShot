[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn wsgi:application --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 300 --worker-class gthread"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
